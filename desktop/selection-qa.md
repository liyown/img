# 图库批量管理本地验收

日期：2026-09-07。macOS 本机，debug 构建，960 × 700 原生窗口。

## 自动化验证

- `cargo test -p img-desktop --quiet`：32 项通过。
- `cargo build -p img-desktop --quiet`：通过。
- 选择状态测试覆盖追加与去重、跨搜索隐藏数量、零搜索结果、取消、退出与失效 ID 清理。
- 复制测试覆盖五种格式、按记录顺序输出、缺失链接计数。
- 清理测试覆盖固定 ID 的重新验证、记录消失、运行中 / 待运行 / 已提交批次保护。
- 用实际 QueueStore 保存失败验证缓存不会删除；成功路径验证缓存被清理，原文件在两种情况下均保留。

## 原生界面验收

使用 `target/selection-qa/data` 隔离目录生成 1,000 条 Done 记录，URL 均为 example.test。配置、全局快捷键目录均隔离，快捷键与检查更新关闭。没有真实上传。

- 图库进入选择模式，勾选 photo-00000 后搜索 photo-00001：已选 1 项、隐藏 1 项。
- 全选当前结果追加 photo-00001，切换列表仍勾选；批量复制提示已复制 2 项。
- 清理确认框显示 2 条、隐藏 1 项，并说明原文件和远端图片保留。取消后磁盘仍有 1,000 条记录，选择保留。
- 再次确认清理后界面及磁盘均为 998 条，目标两条记录及缓存目录消失，其他缓存目录保留；选中数量归零，搜索空结果正常。
- 清空搜索后全选剩余 998 条；列表向下滚动 8 页，后续记录仍选中。切换网格保持选择；图片缩略图打开预览正常。
- 格式菜单选 Markdown 图片后提示已复制 998 项；搜索无匹配词时显示已选 998 项、隐藏 998 项，复制与清理仍可用，全选禁用。
- 「取消全部」「完成」及离开图库再返回均正常。紧凑窗口下操作栏、格式按钮、复选框无重叠或截断。

本次验证没有测量帧时间，也没有执行 Windows / Linux 原生界面验收。选择状态仅驻留内存，无配置或队列格式迁移。

## 排版反馈修订

根据用户截图，将分散的文字按钮收进带细边框的两排操作面板。上排为选中数、隐藏数及全选 / 取消，下排为格式选择及复制 / 清理；复制为主按钮，清理为描边按钮。隐藏数量为零时省略提示。修订过程中发现并移除了重复的清理按钮节点，修复 debug 无障碍树重复 ID 崩溃。

重新构建后在 `target/selection-qa/layout-data` 隔离数据中复查 960 × 700、998 项全选且搜索零结果的场景，按钮完整显示、对齐正常，原生窗口无崩溃。
