interface:
  display_name: "img Image Tools"
  short_description: "Upload, process, and manage images with img"
  default_prompt: "Use $img-uploader to process my image, upload it to my configured storage, and return a Markdown link."
